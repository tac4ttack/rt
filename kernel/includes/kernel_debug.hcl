int		print_vect(const float4 vect)
{
		printf("x = %f\ny = %f\nz = %f\n\n", vect.x, vect.y, vect.z);
		return (1);
}


int		print_vect2(const float4 vect)
{
		printf("x2 = %f\ny2 = %f\nz2 = %f\n\n", vect.x, vect.y, vect.z);
		return (1);
}
